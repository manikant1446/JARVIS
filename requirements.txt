# ── Core: UI, Audio, & Gemini Live 2.0 ──────────────────────────────────────────
PyQt6>=6.6.0
sounddevice>=0.4.6
numpy>=1.26.0
google-genai>=2.8.0
websockets>=13.0
python-dotenv>=1.0.0

# ── Web Search & Autonomous Browser Control ───────────────────────────────────
requests>=2.31.0
beautifulsoup4>=4.12.0
duckduckgo-search>=6.0.0
playwright>=1.40.0

# ── Automation, Keyboard & Cursor Control ─────────────────────────────────────
pyautogui>=0.9.54
pyperclip>=1.8.2
pygetwindow>=0.0.9; sys_platform != "darwin"

# ── Vision, Screen Capture & Media ────────────────────────────────────────────
pillow>=10.0.0
opencv-python>=4.8.0
mss>=9.0.0

# ── System, Security, Files & Documents ───────────────────────────────────────
psutil>=5.9.0
send2trash>=1.8.0
pypdf>=4.0.0
PyPDF2>=3.0.0
python-docx>=1.1.0
python-pptx>=0.6.20
openpyxl>=3.1.0
pdfplumber>=0.11.0
pdf2image>=1.17.0
youtube-transcript-api>=0.6.0

# ── Remote Dashboard (Local Web & Mobile QR Access) ───────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
cryptography>=42.0.0
python-multipart>=0.0.9
qrcode[pil]>=7.4.2

# ── Cloud & OAuth Integrations (Google Mail & Calendar) ───────────────────────
google-api-python-client>=2.100.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0

# ── Smart Home & Device Automation ────────────────────────────────────────────
tinytuya>=1.14.0

# ── macOS Native System Bindings ──────────────────────────────────────────────
pyobjc-core>=10.0; sys_platform == "darwin"
pyobjc-framework-Cocoa>=10.0; sys_platform == "darwin"
pyobjc-framework-Quartz>=10.0; sys_platform == "darwin"

# ── Windows-Only Bindings (Automatically Skipped on macOS / Linux) ────────────
comtypes; sys_platform == "win32"
pycaw; sys_platform == "win32"
win10toast; sys_platform == "win32"
pywinauto; sys_platform == "win32"
pywin32; sys_platform == "win32"
wmi; sys_platform == "win32"

# ── Optional: Local Wake Word ("Hey Jarvis") ──────────────────────────────────
# Opt-in local wake word detection:
# pip install openwakeword
